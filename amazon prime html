<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Prime Demo</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
/* ---------- GLOBAL ---------- */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: Arial, Helvetica, sans-serif;
}

body {
    background: #000;
    color: #fff;
    overflow-x: hidden;
}

.hidden {
    display: none;
}

/* ---------- NAVBAR ---------- */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 40px;
    background: #0f171e;
}

.logo {
    color: #00a8e1;
    font-size: 24px;
    font-weight: bold;
    cursor: pointer;
}

.navbar a {
    color: #fff;
    text-decoration: none;
    margin-left: 20px;
}

/* ---------- HERO ---------- */
.hero {
    height: 90vh;
    background: linear-gradient(to right, #000 40%, rgba(0,0,0,0.4)),
    url("https://images.unsplash.com/photo-1524985069026-dd778a71c7b4") center/cover;
    display: flex;
    align-items: center;
    padding: 40px;
}

.hero-content {
    max-width: 500px;
}

.hero h1 {
    font-size: 48px;
    margin-bottom: 20px;
}

.hero p {
    font-size: 18px;
}

/* ---------- BUTTON ---------- */
.cta-btn {
    display: inline-block;
    margin-top: 25px;
    padding: 15px 30px;
    background: #00a8e1;
    color: #000;
    border: none;
    font-weight: bold;
    cursor: pointer;
    border-radius: 4px;
    transition: transform 0.3s, background 0.3s;
}

.cta-btn:hover {
    background: #008bbf;
    transform: scale(1.05);
}

/* ---------- AUTH ---------- */
.auth-page {
    min-height: 100vh;
    background: radial-gradient(circle at top, #0f171e, #000);
    display: flex;
    justify-content: center;
    align-items: center;
}

.auth-box {
    background: #111;
    padding: 40px;
    width: 350px;
    border-radius: 8px;
    animation: fadeIn 1s ease forwards;
}

.auth-box h2 {
    margin-bottom: 20px;
}

.auth-box input {
    width: 100%;
    padding: 12px;
    margin-bottom: 15px;
    background: #222;
    border: none;
    color: #fff;
}

.auth-box p {
    font-size: 14px;
}

.auth-box a {
    color: #00a8e1;
    cursor: pointer;
    text-decoration: none;
}

/* ---------- ANIMATIONS ---------- */
.fade-in {
    animation: fadeIn 1.2s ease forwards;
}

.slide-in {
    animation: slideIn 1s ease forwards;
}

@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

@keyframes slideIn {
    from { opacity: 0; transform: translateX(-40px); }
    to { opacity: 1; transform: translateX(0); }
}
</style>
</head>

<body>

<!-- NAV -->
<div class="navbar">
    <div class="logo" onclick="showHome()">prime</div>
    <div>
        <a onclick="showLogin()">Sign In</a>
    </div>
</div>

<!-- HOME -->
<section id="home" class="hero fade-in">
    <div class="hero-content slide-in">
        <h1>Welcome to Prime</h1>
        <p>Stream movies & shows, get fast delivery, and more.</p>
        <button class="cta-btn" onclick="showSignup()">Start your free trial</button>
    </div>
</section>

<!-- LOGIN -->
<section id="login" class="auth-page hidden">
    <div class="auth-box">
        <h2>Sign In</h2>
        <input type="email" placeholder="Email">
        <input type="password" placeholder="Password">
        <button class="cta-btn">Sign In</button>
        <p>New to Prime? <a onclick="showSignup()">Create account</a></p>
    </div>
</section>

<!-- SIGNUP -->
<section id="signup" class="auth-page hidden">
    <div class="auth-box">
        <h2>Create Account</h2>
        <input type="text" placeholder="Your name">
        <input type="email" placeholder="Email">
        <input type="password" placeholder="Password">
        <button class="cta-btn">Create account</button>
        <p>Already have an account? <a onclick="showLogin()">Sign in</a></p>
    </div>
</section>

<script>
const home = document.getElementById("home");
const login = document.getElementById("login");
const signup = document.getElementById("signup");

function hideAll() {
    home.classList.add("hidden");
    login.classList.add("hidden");
    signup.classList.add("hidden");
}

function showHome() {
    hideAll();
    home.classList.remove("hidden");
}

function showLogin() {
    hideAll();
    login.classList.remove("hidden");
}

function showSignup() {
    hideAll();
    signup.classList.remove("hidden");
}

/* Button micro interaction */
document.querySelectorAll(".cta-btn").forEach(btn => {
    btn.addEventListener("click", () => {
        btn.style.transform = "scale(0.95)";
        setTimeout(() => btn.style.transform = "scale(1)", 150);
    });
});
</script>

</body>
</html>
Add Prime demo single-file app
