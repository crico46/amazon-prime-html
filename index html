<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Prime Wi-Fi</title>

<style>
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: Arial, Helvetica, sans-serif;
}

body {
    background: #000;
    color: #fff;
}

.hidden {
    display: none;
}

/* Header */
.header {
    background: #0f171e;
    padding: 16px;
    text-align: center;
    font-size: 22px;
    font-weight: bold;
    color: #00a8e1;
}

/* Pages */
.page {
    min-height: calc(100vh - 60px);
    display: flex;
    justify-content: center;
    align-items: center;
    animation: fadeIn 0.6s ease;
}

/* Hero */
.hero {
    padding: 30px;
    text-align: center;
}

.hero h1 {
    font-size: 32px;
    margin-bottom: 15px;
}

.hero p {
    font-size: 16px;
    margin-bottom: 25px;
}

/* Buttons */
.btn {
    padding: 14px 26px;
    background: #00a8e1;
    color: #000;
    border: none;
    border-radius: 4px;
    font-weight: bold;
    cursor: pointer;
    transition: transform 0.2s;
}

.btn:active {
    transform: scale(0.95);
}

/* Auth box */
.box {
    background: #111;
    padding: 30px;
    width: 90%;
    max-width: 320px;
    border-radius: 8px;
}

.box h2 {
    margin-bottom: 15px;
}

.box input {
    width: 100%;
    padding: 12px;
    margin-bottom: 12px;
    background: #222;
    border: none;
    color: #fff;
}

.link {
    color: #00a8e1;
    font-size: 14px;
    cursor: pointer;
}

/* Animations */
@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}
</style>
</head>

<body>

<div class="header">Prime Wi-Fi</div>

<!-- HOME -->
<div id="home" class="page">
    <div class="hero">
        <h1>Connect to Prime Wi-Fi</h1>
        <p>Sign in to continue using the network.</p>
        <button class="btn" onclick="showLogin()">Continue</button>
    </div>
</div>

<!-- LOGIN -->
<div id="login" class="page hidden">
    <div class="box">
        <h2>Sign In</h2>
        <input placeholder="Email (demo only)" disabled>
        <input placeholder="Password (demo only)" disabled>
        <button class="btn" onclick="showSuccess()">Sign In</button>
        <p class="link" onclick="showSignup()">Create account</p>
    </div>
</div>

<!-- SIGNUP -->
<div id="signup" class="page hidden">
    <div class="box">
        <h2>Create Account</h2>
        <input placeholder="Name (demo only)" disabled>
        <input placeholder="Email (demo only)" disabled>
        <button class="btn" onclick="showSuccess()">Continue</button>
        <p class="link" onclick="showLogin()">Back to sign in</p>
    </div>
</div>

<!-- SUCCESS -->
<div id="success" class="page hidden">
    <div class="hero">
        <h1>Connected ✅</h1>
        <p>You may now return to your browser.</p>
    </div>
</div>

<script>
function hideAll() {
    document.querySelectorAll('.page').forEach(p => p.classList.add('hidden'));
}

function showLogin() {
    hideAll();
    document.getElementById('login').classList.remove('hidden');
}

function showSignup() {
    hideAll();
    document.getElementById('signup').classList.remove('hidden');
}

function showSuccess() {
    hideAll();
    document.getElementById('success').classList.remove('hidden');
}
</script>

</body>
</html>
